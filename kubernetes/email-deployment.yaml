apiVersion: apps/v1
kind: Deployment
metadata:
  name: email-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: email
  template:
    metadata:
      labels:
        app: email
    spec:
      containers:
        - name: email
          image: brandonlim99/drivewhere:email_final
          ports:
            - containerPort: 5003
          env:
            - name: hostname
              value : "rabbitmq-service"
            - name: GMAIL_APP_PASS
              valueFrom:
                secretKeyRef:
                  name:  gmail-pass
                  key: GMAIL_APP_PASS

--- 
apiVersion: v1
kind: Service
metadata:
  name: email-service
spec:
  selector:
    app: email
  ports:
    - protocol: TCP
      port: 5003
      targetPort: 5003

